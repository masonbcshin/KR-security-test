# Balanced Trend v2 Tournament

`balanced_trend_krx_etf_v1`과 사전등록 후보 B~E를 동일 데이터·비용·체결 proxy로 비교한다.

결과를 보기 전에 고정한 규칙은 `PROTOCOL.md`가 유일한 판정 기준이다. GitHub Actions는 공개 Yahoo Finance 일봉을 내려받고 `out/`에 결과를 생성해 artifact로 보존한다. 결과 생성 전에는 v2 승격 판정을 내리지 않는다.
